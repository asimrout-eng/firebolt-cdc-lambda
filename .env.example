# Firebolt CDC Lambda Configuration
# Copy this to .env and fill in your values

# AWS Configuration
AWS_REGION=ap-south-1
ENVIRONMENT=prod

# S3 Configuration
S3_BUCKET_NAME=fcanalytics
S3_PREFIX=firebolt_dms_job/

# Lambda Configuration
LAMBDA_FUNCTION_NAME=firebolt-cdc-processor
LAMBDA_TIMEOUT=300
LAMBDA_MEMORY_SIZE=512

# ============================================
# FIREBOLT CREDENTIALS (REQUIRED)
# ============================================
FIREBOLT_ACCOUNT=faircent
FIREBOLT_DATABASE=fair
FIREBOLT_ENGINE=general_purpose_engine
FIREBOLT_USERNAME=your_username@faircent.com
FIREBOLT_PASSWORD=your_password_here

# Firebolt External Location (REQUIRED)
# This is the name of the EXTERNAL LOCATION object in Firebolt
LOCATION_NAME=s3_raw_dms

# Table Keys Configuration (REQUIRED)
TABLE_KEYS_S3_BUCKET=fcanalytics
TABLE_KEYS_S3_KEY=firebolt_dms_job/config/tables_keys.json

# CDC Delete Handling (REQUIRED for proper CDC)
CDC_DELETE_COLUMN=Op
CDC_DELETE_VALUES=D

# ============================================
# IMPORTANT NOTES:
# ============================================
# 1. No quotes needed around values
# 2. TABLE_KEYS_S3_KEY must match where you uploaded the file
# 3. CDC_DELETE_COLUMN is case-sensitive (usually "Op")
# 4. Upload tables_keys.json before deploying:
#    aws s3 cp config/tables_keys.json s3://fcanalytics/firebolt_dms_job/config/
