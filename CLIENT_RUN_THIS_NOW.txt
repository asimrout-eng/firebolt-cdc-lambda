═══════════════════════════════════════════════════════════════════
  🚨 URGENT: Lambda Not Updated - Run These Commands
═══════════════════════════════════════════════════════════════════

PROBLEM: You deployed but Lambda is still running old code with errors

SOLUTION: Run these commands (copy-paste)

═══════════════════════════════════════════════════════════════════
  STEP 1: Get Latest from GitHub
═══════════════════════════════════════════════════════════════════

cd firebolt-cdk-package
git pull origin main

═══════════════════════════════════════════════════════════════════
  STEP 2: Force Lambda Update (Option 2 - Recommended)
═══════════════════════════════════════════════════════════════════

cd firebolt-cdk-package

# Create deployment package
mkdir -p lambda-package
cp lambda/handler.py lambda-package/

# Install dependencies
pip3 install -r lambda/requirements.txt -t lambda-package/ \
  --platform manylinux2014_x86_64 \
  --only-binary=:all: \
  --python-version 3.11

# Create zip
cd lambda-package
zip -r ../lambda-code.zip .
cd ..

# Update Lambda
aws lambda update-function-code \
  --function-name firebolt-cdc-processor \
  --zip-file fileb://lambda-code.zip \
  --region ap-south-1

# Clean up
rm -rf lambda-package lambda-code.zip

echo "✅ Lambda code updated!"

═══════════════════════════════════════════════════════════════════
  STEP 3: Force Lambda Restart
═══════════════════════════════════════════════════════════════════

# Force restart
aws lambda update-function-configuration \
  --function-name firebolt-cdc-processor \
  --description "Force restart at $(date -u +%Y-%m-%dT%H:%M:%S)" \
  --region ap-south-1

echo "⏳ Waiting 2 minutes for new events..."
sleep 120

═══════════════════════════════════════════════════════════════════
  STEP 4: Verify New Code is Running
═══════════════════════════════════════════════════════════════════

aws logs tail /aws/lambda/firebolt-cdc-processor \
  --since 2m \
  --region ap-south-1 \
  | grep -E "Processing S3|✓ MERGE|✗ MERGE|cannot COMMIT" \
  | head -30

LOOK FOR:
  ✅ "Processing S3 key: fair/..."
  ✅ "✓ MERGE completed for <table>"
  ✅ "✓ Dropped staging table"

SHOULD NOT SEE:
  ❌ "cannot COMMIT transaction: no transaction is in progress"

═══════════════════════════════════════════════════════════════════
  STEP 5: Monitor Ongoing
═══════════════════════════════════════════════════════════════════

aws logs tail /aws/lambda/firebolt-cdc-processor \
  --follow \
  --region ap-south-1 \
  | grep -E "Processing|✓ MERGE|✗ MERGE"

Press Ctrl+C to stop monitoring

═══════════════════════════════════════════════════════════════════
  EXPECTED RESULT
═══════════════════════════════════════════════════════════════════

After 5-10 minutes:
  ✅ Success rate: > 95%
  ✅ "cannot COMMIT" errors: 0
  ✅ "cannot be retried" errors: < 5%
  ✅ All tables receiving CDC updates

═══════════════════════════════════════════════════════════════════
  IF STILL NOT WORKING
═══════════════════════════════════════════════════════════════════

Export logs and send:

aws logs filter-log-events \
  --log-group-name /aws/lambda/firebolt-cdc-processor \
  --start-time $(date -u -d '30 minutes ago' +%s)000 \
  --region ap-south-1 \
  > lambda_logs_issue.json

Then send lambda_logs_issue.json for analysis

═══════════════════════════════════════════════════════════════════
  TOTAL TIME: ~6 minutes
═══════════════════════════════════════════════════════════════════

